{
  "compilerOptions": {
    "baseUrl": ".",
    "rootDir": ".",
    "jsx": "preserve",
    "module": "ESNext", // 指定模块代码生成规范，这里为 ECMAScript 标准的最新版
    "declaration": false, // 是否在编译时生成相应的 .d.ts 声明文件
    "noImplicitAny": false, // 是否禁止隐式 any（即没有明确指定类型）的使用
    "removeComments": true, // 是否移除注释信息
    "moduleResolution": "node", // 模块解析策略，这里使用 Node.js 的解析方式
    "ignoreDeprecations": "6.0",
    "esModuleInterop": true, // 是否启用 ES 模块间互操作性
    "target": "es6", // 编译后的目标环境和语言版本，这里为 ECMAScript 6
    "sourceMap": true, // 是否生成源代码映射文件
    "lib": ["ESNext", "DOM"], // 编译所需的库文件列表
    "allowSyntheticDefaultImports": true, // 是否允许导入没有默认导出的模块作为默认导出
    "experimentalDecorators": true, // 是否启用实验性的装饰器特性
    "forceConsistentCasingInFileNames": true, // 强制要求所有文件名中的字符大小写一致，以避免不同平台上的命名冲突
    "resolveJsonModule": true, // 是否允许导入 JSON 文件作为一个模块
    "strict": true, // 开启所有严格模式选项
    "skipLibCheck": true // 是否跳过对声明文件的类型检查
  },
  "exclude": ["node_modules", "**/__tests__", "dist/**"] // 排除指定的文件或目录不进行编译
}
